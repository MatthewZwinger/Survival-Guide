<resources>

    <!--
        Base application theme for API 20+. This theme completely replaces
        AppBaseTheme from BOTH res/values/styles.xml and
        res/values-v11/styles.xml on API 20+ devices.
    -->
    <style name="splashScreenTheme" parent="@android:style/Theme.DeviceDefault.Dialog.NoActionBar">
        <item name="android:windowBackground">@drawable/ic_kt</item>
    </style>

</resources>
